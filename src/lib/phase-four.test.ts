import { readFileSync } from "node:fs";import { resolve } from "node:path";import { describe,expect,it } from "vitest";
const migration=readFileSync(resolve(process.cwd(),"supabase/migrations/20260803190000_product_catalog.sql"),"utf8");
describe("catalog foundation",()=>{it("stores money as integer cents",()=>{expect(migration).toContain("price_cents integer");expect(migration).not.toContain("price_cents real");expect(new Intl.NumberFormat("es-ES",{style:"currency",currency:"EUR"}).format(4.5)).toMatch(/4,50/)});it("enforces publication and image integrity",()=>{expect(migration).toContain("active_variant_required");expect(migration).toContain("published_images_require_alt_text");expect(migration).toContain("product_images_one_primary_idx")});it("ships the 14 EU allergen groups",()=>{const values=migration.slice(migration.indexOf("insert into public.allergens"),migration.indexOf("insert into storage.buckets"));expect((values.match(/\('[a-z]+','/g)??[]).length).toBe(14)});it("keeps staff permissions explicit",()=>{expect(migration).toContain("has_role(''operator'')");expect(migration).toContain("has_role(''admin'')");expect(migration).toContain("product_storage_admin_insert")});it("does not cache admin mutations offline",()=>{const sw=readFileSync(resolve(process.cwd(),"public/sw.js"),"utf8");expect(sw).toContain('url.pathname.startsWith("/admin/")')});});
