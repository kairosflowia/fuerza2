"use client";import Link from "next/link";import {useCart} from "./cart-provider";import {CartIcon} from "@/components/ui/icons";export function CartLink(){const{count}=useCart();return <Link href="/carrito" className="cart-link" aria-label={`Cesta, ${count} artículos`}><CartIcon/>{count>0?<span className="cart-link__count" aria-hidden="true">{count}</span>:null}</Link>}
