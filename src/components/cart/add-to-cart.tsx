"use client";import {useState} from "react";import {useCart} from "./cart-provider";import {Button,Select} from "@/components/ui";
export function AddToCart({productName,variants}:{productName:string;variants:{id:string;name:string;priceCents:number}[]}){const{add}=useCart();const[id,setId]=useState(variants[0]?.id??"");const[q,setQ]=useState(1);if(!variants.length)return null;return <div className="add-cart"><Select id="buy-variant" label="Variante" value={id} onChange={e=>setId(e.target.value)}>{variants.map(v=><option key={v.id} value={v.id}>{v.name}</option>)}</Select><Select id="buy-quantity" label="Cantidad" value={q} onChange={e=>setQ(Number(e.target.value))}>{[1,2,3,4,5,6].map(n=><option key={n}>{n}</option>)}</Select><Button onClick={()=>{const v=variants.find(x=>x.id===id)!;add({variantId:id,productName,variantName:v.name,quantity:q,priceCents:v.priceCents})}}>Añadir a la cesta</Button></div>}
