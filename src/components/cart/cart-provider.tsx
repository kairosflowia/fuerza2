"use client";import {createContext,useCallback,useContext,useEffect,useMemo,useState,type ReactNode} from "react";
export type CartItem={variantId:string;productName:string;variantName:string;quantity:number;priceCents:number;note?:string};type Cart={items:CartItem[];add:(item:CartItem)=>void;setQuantity:(id:string,q:number)=>void;remove:(id:string)=>void;clear:()=>void;count:number;total:number};const Context=createContext<Cart|null>(null);
export function CartProvider({children}:{children:ReactNode}){const[items,setItems]=useState<CartItem[]>([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("fuerza-cart")??"[]"))}catch{}},[]);useEffect(()=>{localStorage.setItem("fuerza-cart",JSON.stringify(items))},[items]);const add=useCallback((item:CartItem)=>setItems(old=>{const found=old.find(x=>x.variantId===item.variantId);return found?old.map(x=>x.variantId===item.variantId?{...x,quantity:Math.min(99,x.quantity+item.quantity),note:item.note||x.note}:x):[...old,item]}),[]);const value=useMemo(()=>({items,add,setQuantity:(id:string,q:number)=>setItems(old=>q<=0?old.filter(x=>x.variantId!==id):old.map(x=>x.variantId===id?{...x,quantity:Math.min(99,q)}:x)),remove:(id:string)=>setItems(old=>old.filter(x=>x.variantId!==id)),clear:()=>setItems([]),count:items.reduce((n,x)=>n+x.quantity,0),total:items.reduce((n,x)=>n+(x.priceCents??0)*x.quantity,0)}),[items,add]);return <Context.Provider value={value}>{children}</Context.Provider>};export function useCart(){const c=useContext(Context);if(!c)throw new Error("CartProvider missing");return c}
