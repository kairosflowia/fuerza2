"use client";import {useState} from "react";import {Elements,PaymentElement,useElements,useStripe} from "@stripe/react-stripe-js";import {loadStripe} from "@stripe/stripe-js";import {Button} from "@/components/ui";const promise=process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY?loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY):null;
function Form({code,token}:{code:string;token:string}){const stripe=useStripe(),elements=useElements(),[busy,setBusy]=useState(false),[error,setError]=useState("");return <form onSubmit={async e=>{e.preventDefault();if(!stripe||!elements)return;setBusy(true);const result=await stripe.confirmPayment({elements,confirmParams:{return_url:`${location.origin}/checkout/pago?pedido=${code}&token=${encodeURIComponent(token)}`}});if(result.error){setError(result.error.message??"No se pudo completar el pago.");setBusy(false)}}}><PaymentElement/><p>No se paga en el punto de recogida.</p><Button type="submit" loading={busy} disabled={!stripe}>Pagar ahora</Button>{error?<p role="alert">{error}</p>:null}</form>}
export function PaymentForm({secret,code,token}:{secret:string;code:string;token:string}){if(!promise)return <p role="alert">El pago todavía no está configurado.</p>;return <Elements stripe={promise} options={{clientSecret:secret,appearance:{theme:"flat"}}}><Form code={code} token={token}/></Elements>}
