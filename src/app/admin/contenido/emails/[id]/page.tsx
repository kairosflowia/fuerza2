import Link from"next/link";import{notFound}from"next/navigation";import{AdminPageHeader}from"@/components/admin/admin-page-header";import{createClient}from"@/lib/supabase/server";export default async function Template({params}:{params:Promise<{id:string}>}){const{id}=await params,db:any=await createClient(),{data}=await db.from("notification_templates").select("*").eq("id",id).maybeSingle();if(!data)notFound();return <><AdminPageHeader title={data.name} description={`${data.key} · versión ${data.version} · ${data.status}`}/><h2>Asunto</h2><p>{data.subject_template}</p><h2>Texto</h2><pre>{data.body_text_template}</pre><h2>Variables</h2><p>{data.required_variables?.join(", ")||"Sin variables obligatorias"}</p>{data.status==="draft"?<Link href={`/admin/contenido/emails/${id}/editar`}>Editar borrador</Link>:<p>Las versiones activas son inmutables. Crea una nueva versión para cambiarla.</p>}</>}
