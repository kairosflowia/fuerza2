import{notFound}from"next/navigation";import{AdminPageHeader}from"@/components/admin/admin-page-header";import{createClient}from"@/lib/supabase/server";export default async function EditTemplate({params}:{params:Promise<{id:string}>}){const{id}=await params,db:any=await createClient(),{data}=await db.from("notification_templates").select("*").eq("id",id).eq("status","draft").maybeSingle();if(!data)notFound();return <><AdminPageHeader title="Editar borrador" description="El HTML no admite scripts ni manejadores JavaScript."/><form><label>Asunto<input defaultValue={data.subject_template}/></label><label>HTML<textarea defaultValue={data.body_html_template}/></label><label>Texto plano<textarea defaultValue={data.body_text_template}/></label><button type="button" disabled>Guardado administrativo pendiente de validación editorial</button></form></>}
