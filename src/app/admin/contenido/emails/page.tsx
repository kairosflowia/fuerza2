import Link from"next/link";import{AdminPageHeader}from"@/components/admin/admin-page-header";import{createClient}from"@/lib/supabase/server";export default async function EmailTemplates(){const db:any=await createClient(),{data}=await db.from("notification_templates").select("id,key,name,locale,status,version,required_variables").order("key").order("version",{ascending:false});return <><AdminPageHeader title="Plantillas de email" description="Versiones auditables de comunicaciones transaccionales."/><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Evento</th><th>Idioma</th><th>Versión</th><th>Estado</th></tr></thead><tbody>{data?.map((template:any)=><tr key={template.id}><td><Link href={`/admin/contenido/emails/${template.id}`}>{template.name}</Link></td><td>{template.locale}</td><td>{template.version}</td><td>{template.status}</td></tr>)}</tbody></table></div><Link href="/admin/contenido/emails/preview">Previsualizar una plantilla</Link></>}
