import{AdminPageHeader}from"@/components/admin/admin-page-header";import{createClient}from"@/lib/supabase/server";import{createIncident}from"../actions";
export default async function Incidents({searchParams}:{searchParams:Promise<{fecha?:string}>}){const date=(await searchParams).fecha??new Date().toISOString().slice(0,10),db:any=await createClient(),{data}=await db.from("production_incidents").select("*").eq("production_date",date).order("created_at",{ascending:false});return <><AdminPageHeader title="Incidencias" description="Problemas operativos conservados hasta su resolución."/><form action={createIncident}><input type="hidden" name="date" value={date}/><label>Tipo<select name="type"><option value="quality_issue">Calidad</option><option value="missing_product">Producto ausente</option><option value="delayed_production">Retraso</option><option value="other">Otro</option></select></label><label>Severidad<select name="severity"><option>low</option><option>medium</option><option>high</option><option>critical</option></select></label><label>Descripción<textarea name="description" required/></label><button>Registrar incidencia</button></form><div className="production-grid">{data?.map((incident:any)=><article className="production-card" key={incident.id}><p className="eyebrow">{incident.severity} · {incident.status}</p><h2>{incident.type}</h2><p>{incident.description}</p>{incident.resolution?<p>Resolución: {incident.resolution}</p>:null}</article>)}</div></>}
