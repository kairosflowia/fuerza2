import{NextResponse}from"next/server";import{createAdminClient}from"@/lib/supabase/admin";import{processNotificationQueue}from"@/lib/notifications/service";import{processPushNotifications}from"@/lib/notifications/push";export const dynamic="force-dynamic";export async function GET(request:Request){if(process.env.CRON_SECRET&&request.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"unauthorized"},{status:401});const db=createAdminClient()as any;const{data:reminders}=await db.rpc("enqueue_pickup_reminders");const processed=await processNotificationQueue(25),push=await processPushNotifications(50);return NextResponse.json({ok:true,reminders,processed:processed.length,succeeded:processed.filter(item=>item.ok).length,push})}
