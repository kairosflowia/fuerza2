import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";export async function POST(req:Request){const db:any=await createClient(),b=await req.json(),{data,error}=await db.rpc("request_subscription_change",{p_subscription_id:b.id,p_type:b.type,p_data:{},p_effective_from:b.effectiveFrom});return error?NextResponse.json({error:"change_rejected"},{status:400}):NextResponse.json({id:data})}
