import {createHash} from "node:crypto";import {NextResponse} from "next/server";import type Stripe from "stripe";import {createAdminClient} from "@/lib/supabase/admin";import {getStripe} from "@/lib/stripe";
export const dynamic="force-dynamic";export async function POST(request:Request){const secret=process.env.STRIPE_WEBHOOK_SECRET;if(!secret)return NextResponse.json({error:"not_configured"},{status:503});const raw=await request.text();let event:Stripe.Event;try{event=getStripe().webhooks.constructEvent(raw,request.headers.get("stripe-signature")??"",secret)}catch{return NextResponse.json({error:"invalid_signature"},{status:400})}const object=event.data.object as Stripe.PaymentIntent|Stripe.Charge;const intent="payment_intent" in object&&typeof object.payment_intent==="string"?object.payment_intent:object.object==="payment_intent"?object.id:"";const amount=object.object==="payment_intent"?object.amount:object.amount;const currency=object.currency;const{error}=await(createAdminClient() as any).rpc("process_payment_event",{p_event_id:event.id,p_event_type:event.type,p_payment_intent:intent,p_amount:amount,p_currency:currency,p_payload_hash:createHash("sha256").update(raw).digest("hex")});return error?NextResponse.json({error:"processing_failed"},{status:500}):NextResponse.json({received:true})}
