import {createHash} from "node:crypto";import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";export const dynamic="force-dynamic";export async function GET(req:Request,{params}:{params:Promise<{code:string}>}){const token=new URL(req.url).searchParams.get("token")??"",hash=createHash("sha256").update(token).digest("hex"),code=(await params).code;const db=createAdminClient() as any;const{data}=await db.from("orders").select("id,public_code,status,payment_status,customer_name,customer_email,customer_phone,collection_date,total_cents,currency,requires_review,pickup_points(name),order_items(product_name_snapshot,variant_name_snapshot,quantity,line_total_cents)").eq("public_code",code).eq("lookup_token_hash",hash).maybeSingle();return data?NextResponse.json(data):NextResponse.json({error:"not_found"},{status:404})}
