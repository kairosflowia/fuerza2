import {createClient} from "@/lib/supabase/server";import {CartPageClient} from "@/components/cart/cart-page";import {Container,Section} from "@/components/ui";export const metadata={title:"Carrito | FUERZA"};export default async function CartPage(){const{data:points}=await(await createClient()).from("pickup_points_public").select("id,name").eq("status","active").order("display_order");return <main id="main-content"><Section><Container><h1>Carrito</h1><CartPageClient points={points??[]}/></Container></Section></main>}
