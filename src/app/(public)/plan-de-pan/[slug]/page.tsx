import Link from"next/link";import{notFound}from"next/navigation";import{Container,Section}from"@/components/ui";import{createClient}from"@/lib/supabase/server";export default async function Plan({params}:{params:Promise<{slug:string}>}){const db:any=await createClient(),{data:p}=await db.from("subscription_plans").select("*,subscription_plan_items(*,product_variants(name,products(name)))").eq("slug",(await params).slug).eq("status","active").eq("is_public",true).maybeSingle();if(!p)notFound();return <main id="main-content"><Section><Container><h1>{p.name}</h1><p>{p.description}</p><ul>{p.subscription_plan_items?.map((i:any)=><li key={i.id}>{i.quantity} × {i.product_variants?.products?.name} · {i.product_variants?.name}</li>)}</ul><p><strong>{(p.price_cents/100).toLocaleString("es-ES",{style:"currency",currency:"EUR"})}</strong> por ciclo, IVA incluido.</p><p>Puedes pausar, retomar o cancelar. Cada ciclo depende de capacidad confirmada.</p><Link className="button button--primary" href={`/plan-de-pan/${p.slug}/configurar`}>Configurar este plan</Link></Container></Section></main>}
