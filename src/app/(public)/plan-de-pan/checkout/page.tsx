import{redirect}from"next/navigation";import{Container,Section}from"@/components/ui";import{getCurrentIdentity}from"@/lib/auth/session";export default async function SubscriptionCheckout(){if(!await getCurrentIdentity())redirect("/cuenta/acceder?next=/plan-de-pan/checkout");return <main id="main-content"><Section><Container><h1>Pago del Plan de Pan</h1><p>Stripe está preparando el método de pago recurrente. La subscripción solo se activará tras el webhook de una factura pagada.</p></Container></Section></main>}
