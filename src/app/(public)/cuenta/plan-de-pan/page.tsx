import Link from"next/link";import{redirect}from"next/navigation";import{Container,EmptyState,Section}from"@/components/ui";import{getCurrentIdentity}from"@/lib/auth/session";import{createClient}from"@/lib/supabase/server";export default async function CustomerSubscriptions(){const identity=await getCurrentIdentity();if(!identity)redirect("/cuenta/acceder?next=/cuenta/plan-de-pan");const db:any=await createClient(),{data:list}=await db.from("subscriptions").select("id,status,next_collection_date,subscription_plans(name,price_cents),pickup_points(name)").eq("customer_id",identity.user.id).order("created_at",{ascending:false});return <main id="main-content"><Section><Container><h1>Mi Plan de Pan</h1>{list?.length?<ul>{list.map((s:any)=><li key={s.id}><Link href={`/cuenta/plan-de-pan/${s.id}`}>{s.subscription_plans?.name}</Link> · {s.status} · próxima recogida {s.next_collection_date??"pendiente"}</li>)}</ul>:<EmptyState title="No tienes un Plan de Pan" description="Consulta los planes cuando estén disponibles."/>}</Container></Section></main>}
